<%= form_for(@vacation) do |f| %>
  <% if @vacation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vacation.errors.count, "error") %> prohibited this vacation from being saved:</h2>

      <ul>
      <% @vacation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :starttime %><br>
    <%= f.date_select :starttime %>
  </div>
  <div class="field">
    <%= f.label :endtime %><br>
    <%= f.date_select :endtime %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.select :user_id, User.all.map{|d| [d.email, d.id]} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
